:root {
    --cor-cidade: #3a4e7a;
    --cor-campo: #5a8f3d;
    --fonte-urbana: 'Urbanist', sans-serif;
    --fonte-rural: 'Patrick Hand', cursive;
}

/* RESET */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    overflow-x: hidden;
    background-color: #f5f5f5;
}

/* CABEÇALHO */
header {
    background: linear-gradient(90deg, var(--cor-cidade) 0%, var(--cor-campo) 100%);
    color: white;
    text-align: center;
    padding: 2rem;
    font-family: var(--fonte-urbana);
    position: relative;
}

header h1 {
    font-size: 2.5rem;
    text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
    animation: pulse 3s infinite;
}

@keyframes pulse {
    0% { transform: scale(1); }
    50% { transform: scale(1.05); }
    100% { transform: scale(1); }
}

/* SEÇÃO DUALIDADE */
.dualidade {
    display: flex;
    min-height: 70vh;
}

.metade {
    width: 50%;
    padding: 2rem;
    transition: all 0.5s ease;
    display: flex;
    flex-direction: column;
    justify-content: center;
}

#cidade {
    background-color: var(--cor-cidade);
    color: white;
    font-family: var(--fonte-urbana);
    align-items: flex-end;
    text-align: right;
}

#campo {
    background-color: var(--cor-campo);
    color: #fff8e1;
    font-family: var(--fonte-rural);
    align-items: flex-start;
    text-align: left;
}

.metade h2 {
    font-size: 2rem;
    margin-bottom: 1.5rem;
    border-bottom: 2px dashed currentColor;
    padding-bottom: 0.5rem;
    display: inline-block;
}

.metade p {
    max-width: 80%;
    line-height: 1.6;
    margin-bottom: 1.5rem;
}

/* IMAGENS */
.img-container {
    width: 200px;
    height: 200px;
    border-radius: 50%;
    overflow: hidden;
    border: 5px solid white;
    box-shadow: 0 4px 8px rgba(0,0,0,0.2);
    margin: 1rem 0;
}

.img-container img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.5s ease;
}

.img-container:hover img {
    transform: scale(1.1);
}

/* BOTÃO */
.transformar {
    position: fixed;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    background: white;
    color: var(--cor-cidade);
    border: none;
    padding: 1rem 2rem;
    border-radius: 50px;
    font-family: var(--fonte-urbana);
    font-weight: bold;
    cursor: pointer;
    box-shadow: 0 4px 15px rgba(0,0,0,0.2);
    z-index: 100;
    transition: all 0.3s ease;
}

.transformar:hover {
    background: var(--cor-campo);
    color: white;
}

/* RODAPÉ */
footer {
    background: #333;
    color: white;
    text-align: center;
    padding: 1.5rem;
    font-family: var(--fonte-urbana);
    font-size: 0.9rem;
}

/* RESPONSIVO */
@media (max-width: 768px) {
    .dualidade {
        flex-direction: column;
    }
    
    .metade {
        width: 100%;
        align-items: center !important;
        text-align: center !important;
    }
    
    .metade p {
        max-width: 100%;
    }
}
